# Backend bind address
INKLY_BIND_ADDR=127.0.0.1:8080

# Where tantivy index is persisted
INKLY_TANTIVY_DIR=./data/tantivy

# JWT secret for HS256 auth (required)
INKLY_JWT_SECRET=change-me

# Max request body size (bytes)
INKLY_MAX_BODY_BYTES=1048576

